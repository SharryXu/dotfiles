#!/usr/bin/env python

# This program is used to print all installed node packages.

import json
import os

rawJsonStr = os.popen("npm list --global --depth 0 --json").read()

packages = json.loads(rawJsonStr)['dependencies'].keys()

for package in packages:
    print(package)
