#!/bin/bash

# Check whether specific folder is empty or not.
# Globals:
#   find
#   print
# Arguments:
#   Folder Path
# Returns:
#   Folder Status

declare -r manual="Usage: is_folder_empty <folder path>"

if [ $# -eq 1 ]; then
  result=$(find $1 -name '*' -maxdepth 1)
  if [[ $result == $1 ]]; then
    echo 0
  else
    echo 1
  fi
else
  echo -e $manual
  exit 1
fi

exit #?
